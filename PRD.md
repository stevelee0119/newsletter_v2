# PRD — 국방·법무 뉴스레터 자동 발송 시스템

| 항목 | 내용 |
|---|---|
| 문서 버전 | v2.0 (2026-08-02) |
| 제품명 | 국방·법무 주요 뉴스 브리핑 자동화 |
| 대상 사용자 | 운영자 본인 (1인 수신) |
| 저장소 | github.com/stevelee0119/newsletter_v2 |

이 문서는 **현재 구현된 프로그램을 그대로 재현할 수 있을 정도의 상세도**를 목표로 한다. 코드가 바뀌면 이 문서(그리고 README.md·SETUP.md)도 함께 갱신한다 — 하단 "문서 동기화 원칙" 참조.

---

## 1. 개요 및 목표

국방·군 법무 및 법조 분야 종사자를 위한 일간 뉴스 브리핑을 **완전 자동으로** 생성·발송하는 시스템.

- 매일 **06:30 이전에 카카오톡·텔레그램으로 뉴스레터 수신** (06:30에 실행 시작이 아니라 06:30에 받아볼 수 있어야 함), **동시에 Notion에도 아카이브 동기화**
- 로컬 PC 전원 여부와 무관하게 동작 (**GitHub Actions** 클라우드 스케줄러 사용, 비용 0원)
- 지정 키워드 52개를 구글 뉴스에서 검색 → **발송 시점 기준 24시간 이내 작성 기사만** 국내 언론 한정 수집 → **3단계 중복·과거 기사 필터링**(로컬 → Gemini → Claude) → 5개 섹션으로 분류 → 과거 작성 사례와 동일한 포맷으로 조판 → 발송

### 성공 기준
1. 평일/주말 매일 06:30 이전 수신율 ≥ 99% (GitHub Actions 지연 감안한 버퍼 설계)
2. 동일 사건 중복 기사 ≤ 5%, 24시간 초과 과거 기사 ≤ 5%
3. 1회 실행 비용: Claude API 약 $0.1~0.2 수준(sonnet-5, 본문 발췌 500자 기준), 인프라 비용 0원, Gemini는 무료 티어로 충분(선택)
4. 1회 실행 시간(수집~발송 준비): 1분 내외(발송 대기 시간 제외) — 원문 본문 수집 단계가 대부분

---

## 2. 사용자 결정 사항 (확정)

| 결정 항목 | 선택 |
|---|---|
| 실행 환경 | GitHub Actions (무료, 전원 불필요) |
| 카카오톡 발송 | 카카오 개발자 API "나에게 보내기" (본인 수신, 무료) |
| 분류·최종 중복판단 | Claude API (기본 모델: `claude-sonnet-5`, thinking 비활성화, config에서 변경 가능) |
| 콘텐츠 기반 중복·과거 기사 판단 | Gemini API (선택, 기본 모델 `gemini-3.5-flash`, `GEMINI_API_KEY` 미설정 시 건너뜀) |
| 뉴스 소스 | 구글 뉴스 RSS (한국판, API 키 불필요) |
| 기사 시효 | 발송 시각 기준 **24시간 이내 작성** 기사만 포함, 원문 페이지 실제 발행 시각으로 2차 재검증 |
| 아카이브 동기화 | 카카오톡 발송과 **동시에** Notion 데이터베이스에 페이지 생성 |

---

## 3. 기능 요구사항

### 3.1 수집 (Collector) — `src/collect.py`
- 소스: Google News RSS 한국판 (`hl=ko&gl=KR&ceid=KR:ko`), 키워드별 개별 쿼리(`"키워드" when:1d`)
- 키워드 (config.yaml에서 관리, 총 52개):
  - **특검**: 특검
  - **국방**: 국방부, 합참, 육군, 해군, 공군, 해병대, 법무관, 군사경찰, 장교, 부사관, 병사, 후임병, 방사청, 무기체계, 미래전, 현대전, 국제법, 국제형사재판소, 국제사법재판소
  - **법조·공직**: 대통령, 청와대, 본회의, 법사위, 국방위, 운영위, 법무부, 법제처, 변협, 법무법인, 로펌, 대법원, 헌재, 사법부, 양형위, 법관, 재판관, 대검, 검찰, 경찰청, 공소청, 중수청, 공수처
  - **판결**: 판결, 재판부 판결, 재판부 선고, 법원 판결, 법원 선고
  - **일정**: 오늘의 법조, 오늘의 사회, 오늘의 국회, 오늘의 인사
- **1차 시효 필터(수집 단계)**: 발송 시각 기준 24시간 이내에 작성(RSS `pubDate`)된 기사만 포함. 발행일을 확인할 수 없는 기사는 안전하게 제외
- 병렬 수집(ThreadPoolExecutor, 8 workers)
- 대한민국 언론 한정: 한국판 RSS + 언론사 차단 목록(config) 이중 필터

### 3.2 원문 링크·본문 복원 (`resolve_links`) — 1차 중복 제거 직후 실행
1차(로컬) 중복 제거를 통과한 후보 전체(수십~백여 건)를 대상으로, 이후 단계(Gemini 2단계·Claude 분류)가 사용할 정보를 준비한다:
- 구글 뉴스 리다이렉트 링크를 언론사 원문 링크로 복원(`googlenewsdecoder` 우선, 실패 시 redirect 추적, 그래도 실패하면 구글 링크 유지)
- 원문 페이지를 1회 요청(timeout 12초)해서:
  - **실제 발행 시각**: `article:published_time` 등 메타태그를 정규식으로 추출 → `page_published`
  - **본문 전체**: `trafilatura.extract()`로 광고·네비게이션을 제거한 순수 본문 추출 → `full_text`
- 개별 기사 단위로 실패해도 예외를 전파하지 않고 해당 필드만 `None`으로 남김(폴백은 각 소비 단계 책임)
- 병렬 처리(ThreadPoolExecutor, 12 workers)

### 3.3 중복·과거 기사 필터링 — 3단계 설계
토큰·시간 낭비를 막기 위해 **저비용 1차(로컬) → 중간 비용 2차(Gemini, 선택) → 최종 안전망 3차(Claude)** 로 분리:

1. **1차 (로컬, `src/dedupe.py::dedupe`, 무료, 제목 기반)**
   - URL 정규화 중복 제거 → 제목 정규화([단독] 등 태그 제거, 기호 제거)
   - 언론사 순위(`media_rank`) → 상세도(제목+요약+본문 길이) 순 정렬 후 그리디 클러스터링
   - 두 제목을 같은 사건으로 판단하는 조건(OR):
     a. `rapidfuzz.fuzz.token_set_ratio ≥ dedupe_threshold`(기본 82)
     b. 핵심어(2글자 이상 어절) 겹침 비율 `≥ dedupe_overlap_threshold`(기본 40, 최소 공유 개수 `dedupe_min_shared_tokens`=2)
     c. **특이 공유 어절(distinctive shared token)**: 이번 배치 전체에서 드물게 등장하는 단어(인물명·훈련명 등)를 하나라도 공유하면 중복으로 판단 — idf = `ln((전체건수+1)/(그 단어 등장 건수+1)) ≥ dedupe_distinctive_idf`(기본 3.0). "특검", "조사"처럼 흔한 단어는 문서빈도가 높아 이 신호에 기여하지 않는다.
   - 카테고리는 판단 기준에서 제외(같은 사건이 "특검"과 "대통령" 등 다른 키워드로 각각 수집될 수 있으므로) — 후보 상한 적용에만 사용
   - 1차 후 카테고리별 후보 상한(`candidate_limits`: 특검 25 / 국방 60 / 법조 60 / 판결 25 / 일정 15, 총 ≤185건)으로 다음 단계 입력 규모 고정
2. **2차 (Gemini, `src/dedupe.py::gemini_content_dedupe`, 선택, 본문 기반)**
   - `GEMINI_API_KEY` 미설정 또는 `google-genai` 패키지 미설치 시 건너뜀(파이프라인 나머지는 정상 진행)
   - 각 후보의 제목 + 본문 발췌(`_content_excerpt`, `full_text` 우선·없으면 `description`, 최대 `CONTENT_EXCERPT_CHARS`=500자)를 Gemini에 전달
   - 구조화 출력(`response_json_schema`)으로 두 가지를 동시에 판단:
     a. `duplicate_groups`: 제목이 크게 달라도 본문상 같은 사건인 기사 그룹(대표 선정은 1차와 동일한 media_rank/상세도 기준)
     b. `stale_ids`: 본문에 명시된 과거 날짜를 근거로, 새 진전 없이 재보도된 과거 기사(단순 배경 설명이나 오늘의 후속 진전 기사는 제외 — 애매하면 탈락시키지 않음)
   - API 실패(429/네트워크 오류 등) 시 경고 로그만 남기고 원본 후보 목록을 그대로 반환(그레이스풀 스킵)
3. **3차 (Claude, `src/classify.py::classify`, 최종 안전망)**
   - 1·2차를 통과한 후보에 대해 섹션 분류·중요도 정렬과 **동시에** 같은 중복/과거 기사 판단을 마지막으로 한 번 더 수행(1·2차가 완벽하지 않으므로)
   - 페이로드: id, 제목, 언론사, 검색 키워드, 본문 발췌(2차와 동일한 `_content_excerpt`, 500자)
   - `thinking: {"type": "disabled"}` 명시 — sonnet-5는 thinking이 기본 활성화라 비활성화하지 않으면 `max_tokens`를 소진해 응답이 비는 문제가 있었음(§6 리스크 참조)

### 3.4 분류 (Classifier) — `src/classify.py`
- Claude API 1회 호출 (structured output, JSON 스키마 강제)로 섹션 배치 + 중요도 정렬 + 중복/과거 기사 최종 확인 + 무관 기사 탈락 + 하루 요약을 동시 수행
- 섹션 구조 (과거 사례와 동일):
  - 1️⃣ 5대 특검 분야
  - 2️⃣ 국방·군 법무 동향 — [국방 일반] [북한·한미동맹] [방산·미래전 등] [국제·국제법 등]
  - 3️⃣ 법조·공직·수사기관 동향 — [국회·정부] [법조일반] [사법·수사기관]
  - 4️⃣ 🧑‍⚖️ 오늘의 주요 판결
  - 5️⃣ 🗓️ 오늘의 주요 일정 종합 (+ 고정 링크: 네이버 주요사설)
- 서브섹션별 최대 기사 수는 config(`section_limits`)에서 관리
- **`daily_summary`**: 같은 호출에서 오늘 전체 기사 중 가장 중요한 흐름을 130자 이내 한국어 한 문장으로 요약(구체적 사건·인물 중심, 상투적 문구 금지) — 카카오톡 요약 텍스트에 그대로 노출됨. 추가 API 호출 없이 분류 호출 1회에 통합

### 3.5 조판 (Formatter) — `src/format_newsletter.py`
- 과거 작성 사례 포맷 재현: `🗞️ ['26. 8. 1.(금) 국방·법무 주요 뉴스 브리핑] 🗞️`, 섹션 이모지, `• 제목(언론사)` + 링크, 말미 `//끝//`
- 산출물 2종:
  - `newsletters/YYYY-MM-DD.md` — 저장소에 커밋되는 전문 아카이브
  - `out/summary.txt` — 카카오용 요약(`format_summary`): 헤드라인 + `daily_summary` + `▶ 전체 보기: <링크>`. **링크는 항상 예산을 먼저 확보**해 200자 제한 안에서 절대 잘리지 않도록 하고, 남는 길이만큼만 daily_summary를 채움(초과 시 말줄임표로 자름)

### 3.6 신선도 이중 검증 (`filter_resolved_freshness`)
Google News의 `pubDate`는 원문 재크롤링/업데이트 시점으로 갱신되어 실제 최초 발행일보다 최신으로 표시되는 경우가 있다. 분류 완료 후 최종 선정된 기사에 대해 다음 순서로 재검증한다:

1. **`page_published`(원문 페이지 메타태그, 시간 단위 정밀도)**가 있으면 최우선 사용 — `lookback_hours` 기준 엄격 컷오프(여유 없음). 네이버 뉴스처럼 URL에 날짜가 없는 언론사에도 적용 가능해, 이전 버전에서 있었던 "URL에 날짜가 없으면 신선도 재검증을 건너뛰는" 구멍을 메움
2. 없으면 **URL에 새겨진 발행일**(국내 언론사 URL 관례, `YYYYMMDD` 패턴, 날짜 단위 정밀도라 하루 여유를 둠)로 보조 판단
3. 둘 다 못 구하면(추출 실패) 통과시킨다 — 수집 단계에서 이미 Google `pubDate` 기준 24시간 이내로 필터링된 상태이므로

### 3.7 발송 (Deliverer) — `src/deliver.py`
- **카카오톡 (1순위)**: `나에게 보내기` API, `object_type: "text"` 템플릿(200자 제한).
  - `text` 필드: 헤드라인 + daily_summary + **링크를 텍스트로도 그대로 노출**(카카오톡이 URL을 자동 하이퍼링크 처리하므로 버튼 렌더링 여부와 무관하게 항상 보이도록)
  - `buttons` 배열(1개, `title: "전체 보기"`)로 버튼도 함께 제공 — 레거시 `button_title` 단일 필드 대신 사용(카카오 문서상 더 최신 필드이며 `buttons`가 우선 적용됨)
  - 액세스 토큰은 리프레시 토큰으로 매회 자동 갱신, 갱신된 리프레시 토큰은 GitHub Secret에 자동 재저장(GH_PAT 설정 시) → 무인 운영 가능
- **텔레그램 (2순위)**: 봇 API로 **전문(full text)** 발송 (4,000자 단위 분할)
- **Notion 동기화 (카카오톡과 동시 실행)**: 카카오 발송 시도와 같은 시점에 Notion 데이터베이스에 당일 뉴스레터 페이지를 생성(전문을 문단 블록으로 변환, 100블록 초과 시 자동 배치 추가). 아카이브 목적이므로 실패해도 카카오·텔레그램 발송 성공 여부에는 영향 없음(별도 로그만 남김)
- 발송 모드 (env `DELIVERY_MODE`):
  - `all` (기본): 카카오+Notion 동시 시도 후 텔레그램도 발송
  - `fallback`: 카카오 시도 → 실패 시에만 텔레그램 (Notion은 모드와 무관하게 항상 동시 시도)
- **수신 채널(카카오톡·텔레그램) 모두 실패 시** 워크플로 실패 처리. Notion 실패는 수신 실패로 간주하지 않음

### 3.8 스케줄링 (06:30 수신 보장 설계)
GitHub Actions `schedule` 이벤트는 `workflow_dispatch`(API 직접 트리거, 실측 수 초 내 시작)와 달리 별도의
지연된 큐를 타는 것으로 실측 확인됨(2026-08-02~07 실측 지연 53→53→66→73→80→92분+로 계속 악화,
목표 시각을 05:40→05:27로 앞당겨도 지연 폭 자체는 줄지 않음 — 즉 목표 시각과 무관한 큐 정체). 대응:

1. **본 실행 4회 분산 시도**: UTC 20:27/20:35/20:45/20:55 (KST 05:27/05:35/05:45/05:55) — `generate()`가
   당일 아카이브 존재 시 즉시 스킵하므로, 가장 먼저 지연 없이 걸린 1회만 실제로 수집·필터링·분류·조판을
   수행하고 **KST 06:28까지 대기했다가 발송** (지연으로 06:28을 넘겨 시작된 경우 즉시 발송), 나머지 3회는
   1초 내 스킵(중복 발송 없음)
2. **백업 실행**: UTC 21:45 (KST 06:45) — 4회 시도가 모두 실패/미발화한 경우의 마지막 안전망. 당일
   뉴스레터가 이미 커밋되어 있으면 즉시 종료(중복 발송 방지), 없으면 생성 후 즉시 발송
3. `workflow_dispatch`로 수동 실행 가능 (스케줄 지연과 무관하게 즉시 시작됨)
4. 발송 시각·대기 상한은 env로 조정 가능 (`TARGET_SEND_TIME`, `MAX_WAIT_MIN`)
5. 근본 해결책으로는 외부 스케줄러(예: cron-job.org)가 `workflow_dispatch` REST API를 직접 호출하는
   방식이 검토됨(`schedule` 큐를 완전히 우회) — 외부 서비스 의존성 추가가 필요해 현재는 보류, 4회 분산
   시도로 우선 대응 중

---

## 4. 시스템 아키텍처

```
GitHub Actions (cron 05:27/05:35/05:45/05:55 KST 4회 분산 시도)
 ├─ 1. collect       : Google News RSS × 52 키워드 (병렬, 24시간 이내 작성분만) ──> 원시 기사 목록
 ├─ 2. dedupe        : 로컬 유사도 클러스터링(rapidfuzz+특이단어) ──> 후보 ≤185건
 ├─ 3. resolve_links : 후보 전체의 원문 링크·본문 전체·실제 발행 시각 복원(병렬)
 ├─ 4. gemini dedupe : (선택) 본문 기반 중복 묶기 + 과거 기사 판단
 ├─ 5. classify      : Claude API 1회 호출 (분류+중복/과거기사 최종확인+정렬+daily_summary, JSON 강제)
 ├─ 6. freshness     : 최종 선정 기사의 실제 발행 시각 2차 재검증
 ├─ 7. format        : 뉴스레터 조판 ──> newsletters/YYYY-MM-DD.md 커밋
 ├─ 8. wait          : KST 06:28까지 대기
 └─ 9. deliver       : 카카오 "나에게 보내기"(요약+링크, 텍스트+버튼) + Notion 동기화(동시) + 텔레그램(전문)
```

| 구성요소 | 파일 |
|---|---|
| 설정(키워드·섹션·상한·언론사순위·모델) | `config.yaml` |
| 수집·링크복원·본문추출·신선도재검증 | `src/collect.py` |
| 1차 로컬 중복 제거 + 2차 Gemini 콘텐츠 필터 | `src/dedupe.py` |
| 3차 분류 (Claude API, 최종 안전망) | `src/classify.py` |
| 조판 | `src/format_newsletter.py` |
| 발송 (카카오·텔레그램·Notion·시크릿 갱신) | `src/deliver.py` |
| 오케스트레이션 (generate / send) | `src/main.py` |
| 스케줄러 | `.github/workflows/newsletter.yml` |
| 카카오 최초 토큰 발급 도우미 | `scripts/kakao_auth.py` |

### 주요 Python 의존성 (`requirements.txt`)
| 패키지 | 용도 |
|---|---|
| `requests` | HTTP (RSS, 링크 해석, 본문 fetch, 각 API 호출) |
| `PyYAML` | config.yaml 로드 |
| `rapidfuzz` | 1차 로컬 중복 제거 유사도 계산 |
| `anthropic` | Claude API (분류) |
| `google-genai` | Gemini API (2단계 콘텐츠 필터, 선택) |
| `trafilatura` | 원문 페이지에서 본문 전체 추출 |
| `PyNaCl` | GitHub Secret 암호화(libsodium sealed box) |
| `googlenewsdecoder` | 구글 뉴스 리다이렉트 링크 디코딩 |

### 필요한 GitHub Secrets
| Secret | 용도 | 필수 |
|---|---|---|
| `ANTHROPIC_API_KEY` | Claude API (분류) | ✅ |
| `GEMINI_API_KEY` | Gemini API (2단계 콘텐츠 필터) | 선택 |
| `KAKAO_REST_API_KEY` | 카카오 앱 REST 키 | 카카오 사용 시 |
| `KAKAO_REFRESH_TOKEN` | 카카오 토큰 자동 갱신 | 카카오 사용 시 |
| `KAKAO_CLIENT_SECRET` | 카카오 앱의 [카카오 로그인 > 보안]에서 Client Secret이 활성화된 경우 | 해당 시에만 |
| `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` | 텔레그램 발송 | 텔레그램 사용 시 |
| `NOTION_API_KEY` | Notion 인테그레이션 토큰 | Notion 사용 시 |
| `NOTION_DATABASE_ID` | 동기화 대상 Notion 데이터베이스 ID | Notion 사용 시 |
| `GH_PAT` | 카카오 리프레시 토큰 회전 시 Secret 자동 갱신 | 권장 |

### 주요 config.yaml 키
| 키 | 설명 | 기본값 |
|---|---|---|
| `model` | 분류에 사용할 Claude 모델 | `claude-sonnet-5` |
| `gemini_model` | 2단계 콘텐츠 필터에 사용할 Gemini 모델 | `gemini-3.5-flash` |
| `lookback_hours` | 수집 시효(시간) | 24 |
| `candidate_limits` | 1차 중복 제거 후 카테고리별 후보 상한 | 특검25/국방60/법조60/판결25/일정15 |
| `dedupe_threshold` | 제목 편집거리 유사도 임계값(rapidfuzz, 0~100) | 82 |
| `dedupe_overlap_threshold` / `dedupe_min_shared_tokens` | 핵심어 겹침 비율 보조 판단 | 40 / 2 |
| `dedupe_distinctive_idf` | 특이 공유 어절 판단 임계값(idf) | 3.0 |
| `section_limits` | 섹션별 최대 기사 수 | — |
| `media_rank` | 언론사 우선순위(중복 대표 선정) | — |
| `source_blocklist` | 수집 제외 언론사 | `[]` |
| `fixed_links` | 뉴스레터 말미 고정 링크 | 주요사설(네이버) |

`src/dedupe.py`의 `CONTENT_EXCERPT_CHARS`(기본 500) — Gemini·Claude에 넘기는 본문 발췌 길이. 코드 상수이며 config.yaml에는 없음(비용에 직접 영향, §6 참조).

---

## 5. 비기능 요구사항

- **비용**: 인프라 0원(공개 저장소 기준 Actions 무제한). Claude sonnet-5 기준 실행당 대략 $0.1~0.2(본문 발췌 500자 포함), 월 예상 ~$5~10. Gemini는 무료 티어로 하루 1회 실행량 충분(단, Google Cloud 계정 설정에 따라 할당량이 0으로 나오는 경우가 있음 — SETUP.md 참조)
- **토큰/시간 상한**: 후보 185건 상한(1차) → Gemini(선택)로 추가 축소 → Claude 호출 1회. 본문 발췌는 `CONTENT_EXCERPT_CHARS`로 상한 고정
- **안정성**: RSS 개별 키워드 실패는 건너뛰고 계속, 링크 복원·본문 추출·발행시각 추출 실패는 각각 그레이스풀 폴백, Gemini 실패는 파이프라인 전체에 영향 없이 스킵, 백업 크론으로 이중화
- **보안**: 모든 키는 GitHub Secrets, 저장소에 평문 저장 금지

---

## 6. 제약사항 및 리스크

| 리스크 | 내용 및 대응 |
|---|---|
| "가장 열람 많은 기사" 판단 | RSS는 조회수를 제공하지 않음 → **주요 언론사 순위 + 기사 상세도**를 대리 지표로 사용 |
| naver.me 단축링크 | 네이버 비공식 단축 API가 없어 재현 불가 → 언론사 원문 링크 사용 |
| 카카오 텍스트 200자 제한 | 요약+링크는 항상 링크 우선 예산 확보, 전문은 텔레그램/GitHub 아카이브로 제공 |
| **GitHub Actions `schedule` 지연 (계속 악화)** | 목표 시각을 05:40→05:27로 앞당겨도 지연 폭(53→53→66→73→80→92분+)이 줄지 않음을 실측 확인 — `workflow_dispatch`는 매번 수 초 내 시작되는 반면 `schedule`은 별도의 정체된 큐를 타는 것으로 판단(목표 시각과 무관). 시각 재조정 대신 05:27~05:55 사이 4회 분산 시도로 대응(§3.8) + 06:45 백업 크론. 근본 해결책(외부 스케줄러의 `workflow_dispatch` 직접 호출)은 검토 중, 미적용 |
| 카카오 리프레시 토큰 만료(2개월) | 매 실행 시 갱신, 회전된 토큰 자동 재저장(GH_PAT). 미설정 시 텔레그램으로 갱신 안내 발송 |
| 구글 뉴스 링크 복원 로직 변경 | 서드파티 디코더 실패 시 구글 리다이렉트 링크로 폴백(클릭 시 정상 이동) |
| 사설 저장소일 경우 | Actions 무료 2,000분/월 한도 내 운영 가능하나 대기시간 포함 시 빠듯 → **공개 저장소 권장** |
| RSS `pubDate` 누락/부정확 | 발행일 확인 불가 기사는 수집 단계에서 엄격 배제. `pubDate`가 부정확한 경우는 §3.6 신선도 이중 검증으로 보완 |
| **URL에 발행일이 없는 언론사(네이버 등)** | 이전 버전은 URL 임베디드 날짜에만 의존해 이 경우 신선도 재검증이 무력화됐음 → 원문 페이지 메타태그 기반 실제 발행 시각(`page_published`)을 최우선 신호로 추가해 해결 |
| **본문 전체 수집에 따른 비용 증가** | 2단계(Gemini)·3단계(Claude) 모두 본문 발췌를 사용하면서 Claude 입력 토큰이 크게 증가한 사례 실측(후보 133건, `CONTENT_EXCERPT_CHARS`=1200자 기준 입력 ~13만 토큰, 실행당 ~$0.3). `CONTENT_EXCERPT_CHARS`를 500자로 낮춰 비용을 예산 범위로 재조정 — 늘릴수록 판단 품질↑·비용↑ 트레이드오프이므로 필요 시 재조정 |
| **sonnet-5 thinking 기본 활성화** | thinking을 명시적으로 끄지 않으면 구조화 출력(JSON) 생성 전에 `max_tokens`를 소진해 빈 응답(StopIteration)으로 실패한 사례 있음 → `classify.py`에서 `thinking: {"type": "disabled"}` 명시로 해결 |
| **Gemini 무료 할당량 0 문제** | 일부 Google Cloud 프로젝트/계정에서 새 프로젝트로 키를 재발급해도 `quota_limit_value: 0`이 지속되는 사례 확인(결제 계정 연결·1시간 경과 후에도 미해결, 최종적으로 키를 재발급하자 해소) → 코드는 이 경우도 그레이스풀 스킵으로 처리하므로 나머지 파이프라인엔 영향 없음. 해결은 SETUP.md 참조 |
| **Gemini 모델 버전 노후화(404)** | 신규 발급 키/프로젝트에서는 구버전 모델이 "no longer available to new users"로 404를 반환하는 사례 확인(`gemini-2.5-flash` → `gemini-3.5-flash`로 대응) → `gemini_model` config 값을 계정에서 실제 사용 가능한 모델로 주기적으로 갱신 필요, 확인 안 되면 이 역시 그레이스풀 스킵 |
| Notion 데이터베이스 title 속성명 상이 | 사용자마다 "이름"/"Name" 등으로 다를 수 있어, 매 실행 시 DB 스키마를 조회해 title 타입 속성을 동적으로 탐지 |
| Notion API 블록 수 제한(요청당 100개) | 전문을 문단 블록으로 변환 후 최초 100개는 페이지 생성 시, 나머지는 100개씩 배치로 append |
| **원문 페이지 접근 실패(봇 차단 등)** | `resolve_links`가 개별 기사 단위로 실패해도 예외 전파 없이 `full_text`/`page_published`를 `None`으로 남기고 계속 진행 — 해당 기사는 RSS 요약(description) 및 Google `pubDate` 기준으로만 판단됨(신뢰도 하락은 감내) |

---

## 7. Notion 연동 세부사항

- **트리거**: `deliver()` 오케스트레이션 내에서 카카오톡 발송과 같은 시점에 `send_notion()` 호출 (발송 모드 `DELIVERY_MODE`와 무관하게 항상 시도)
- **인증**: Notion Internal Integration 토큰(`NOTION_API_KEY`) + 대상 데이터베이스 ID(`NOTION_DATABASE_ID`). 사용자가 Notion에서 인테그레이션을 데이터베이스(또는 워크스페이스)에 "연결"해야 함(SETUP.md 참조)
- **페이지 생성**: 데이터베이스의 title 속성에 `YYYY-MM-DD 국방·법무 주요 뉴스 브리핑` 제목 설정, 본문은 발송 전문을 줄 단위 문단 블록으로 변환하여 첨부
- **실패 처리**: Notion 동기화 실패는 로그로만 기록하고 워크플로 전체 실패로 취급하지 않음(카카오·텔레그램이 실제 수신 채널이므로)

---

## 8. 마일스톤

| 단계 | 내용 | 상태 |
|---|---|---|
| M1 | PRD 확정 | 본 문서 |
| M2 | 수집·중복제거·분류·조판 파이프라인 구현 | v1.0 포함 |
| M3 | 카카오·텔레그램 발송 + 토큰 자동 갱신 | v1.0 포함 |
| M4 | GitHub Actions 스케줄 + 백업 크론 | v1.0 포함 |
| M5 | Notion 동기화 + 24시간 시효 필터 + 판결 키워드 추가 | v1.1 포함 |
| M6 | 사용자 설정(카카오 앱 생성, Notion 인테그레이션, Secrets 등록) — SETUP.md 참조 | 사용자 작업 |
| M7 | sonnet-5 전환(thinking 비활성화) + 카카오 링크 텍스트 노출 + daily_summary | v2.0 포함 |
| M8 | 3단계 필터링(로컬 특이단어 신호 + Gemini 콘텐츠 필터 + Claude 최종 안전망) + 신선도 이중 검증(페이지 메타태그) | v2.0 포함 |
| 추후 | 임시 서브섹션 운영, 수신자 확대(카카오 채널), 품질 튜닝, Gemini 할당량 이슈 해결 후 비용 재측정 | 백로그 |

---

## 9. 문서 동기화 원칙

프로그램(코드·설정·워크플로)이 바뀌면 아래 세 문서 중 영향받는 부분을 함께 갱신한다 — 순서는 상관없지만 같은 커밋/작업 단위에서 처리한다.

| 문서 | 갱신 대상 |
|---|---|
| `PRD.md`(본 문서) | 요구사항·아키텍처·파이프라인 단계·config 키·Secrets·리스크가 바뀌었을 때 — "지금 구현을 그대로 재현할 수 있는 상세도" 유지가 기준 |
| `README.md` | 기능 요약·동작 방식 다이어그램·프로젝트 구조·설정표·비용·발송 채널표가 바뀌었을 때 |
| `SETUP.md` | 새 Secret·새 외부 서비스 연동·설정 절차·트러블슈팅이 추가/변경됐을 때 |

기준: **"이 변경으로 인해 세 문서 중 하나라도 사실과 달라졌는가?"** — 그렇다면 같은 작업에서 함께 고친다.
