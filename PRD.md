# PRD — 국방·법무 뉴스레터 자동 발송 시스템

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 (2026-08-01) |
| 제품명 | 국방·법무 주요 뉴스 브리핑 자동화 |
| 대상 사용자 | 운영자 본인 (1인 수신) |
| 저장소 | github.com/stevelee0119/newsletter_v2 |

---

## 1. 개요 및 목표

국방·군 법무 및 법조 분야 종사자를 위한 일간 뉴스 브리핑을 **완전 자동으로** 생성·발송하는 시스템.

- 매일 **06:30 이전에 카카오톡·텔레그램으로 뉴스레터 수신** (06:30에 실행 시작이 아니라 06:30에 받아볼 수 있어야 함)
- 로컬 PC 전원 여부와 무관하게 동작 (**GitHub Actions** 클라우드 스케줄러 사용, 비용 0원)
- 지정 키워드 47개를 구글 뉴스에서 검색 → 국내 언론 기사만 수집 → 중복 제거 → 5개 섹션으로 분류 → 과거 작성 사례와 동일한 포맷으로 조판 → 발송

### 성공 기준
1. 평일/주말 매일 06:30 이전 수신율 ≥ 99% (GitHub Actions 지연 감안한 버퍼 설계)
2. 동일 사건 중복 기사 ≤ 5%
3. 1회 실행 비용: Claude API 약 10~50원 수준 (Haiku 모델), 인프라 비용 0원
4. 1회 실행 시간(수집~발송 준비): 5분 이내 (발송 대기 시간 제외)

---

## 2. 사용자 결정 사항 (확정)

| 결정 항목 | 선택 |
|---|---|
| 실행 환경 | GitHub Actions (무료, 전원 불필요) |
| 카카오톡 발송 | 카카오 개발자 API "나에게 보내기" (본인 수신, 무료) |
| 분류·중복판단 | Claude API (기본 모델: `claude-haiku-4-5`, config에서 변경 가능) |
| 뉴스 소스 | 구글 뉴스 RSS (한국판, API 키 불필요) |

---

## 3. 기능 요구사항

### 3.1 수집 (Collector)
- 소스: Google News RSS 한국판 (`hl=ko&gl=KR&ceid=KR:ko`), 키워드별 개별 쿼리
- 키워드 (config.yaml에서 관리, 총 47개):
  - **특검**: 특검
  - **국방**: 국방부, 합참, 육군, 해군, 공군, 해병대, 법무관, 군사경찰, 장교, 부사관, 병사, 후임병, 방사청, 무기체계, 미래전, 현대전, 국제법, 국제형사재판소, 국제사법재판소
  - **법조·공직**: 대통령, 청와대, 본회의, 법사위, 국방위, 운영위, 법무부, 법제처, 변협, 법무법인, 로펌, 대법원, 헌재, 사법부, 양형위, 법관, 재판관, 대검, 검찰, 경찰청, 공소청, 중수청, 공수처
  - **일정**: 오늘의 법조, 오늘의 사회, 오늘의 국회, 오늘의 인사
- 수집 범위: 최근 28시간 (전일 새벽~당일 새벽, 누락 방지용 여유 포함)
- 병렬 수집(스레드풀)으로 전체 수집 2분 이내
- 대한민국 언론 한정: 한국판 RSS + 언론사 차단 목록(config) 이중 필터
- 구글 뉴스 리다이렉트 링크는 가능한 경우 언론사 원문 링크로 복원 (실패 시 구글 링크 유지)

### 3.2 중복 제거 (Deduplicator) — 2단계 설계
토큰·시간 낭비를 막기 위해 **저비용 1차(로컬) + 고품질 2차(LLM)** 로 분리:

1. **1차 (로컬, 무료)**: URL 정규화 중복 제거 → 제목 정규화([단독], 기호 제거) → rapidfuzz 유사도(token_set_ratio ≥ 82) 그리디 클러스터링. 클러스터 대표 기사 선정 기준(우선순위):
   1. 주요 언론사 순위 (config의 media_rank — RSS는 조회수를 제공하지 않으므로 "열람 많은 기사"의 대리 지표로 언론사 도달률 순위 사용, §6 제약 참조)
   2. 기사 상세도 (제목·요약 길이)
2. **1차 후 카테고리별 후보 상한** (특검 25 / 국방 60 / 법조 60 / 일정 15, 총 ≤160건)으로 LLM 입력 토큰 상한 고정
3. **2차 (LLM)**: 분류와 동시에 의미 기반 중복(제목이 달라도 같은 사건) 최종 제거 — 별도 호출 없이 분류 호출 1회에 통합

### 3.3 분류 (Classifier)
- Claude API 1회 호출 (structured output, JSON 스키마 강제)로 아래 서브섹션에 기사 배치 + 중요도 정렬 + 의미 중복 탈락 + 무관 기사 탈락을 동시 수행
- 섹션 구조 (과거 사례와 동일):
  - 1️⃣ 5대 특검 분야
  - 2️⃣ 국방·군 법무 동향 — [국방 일반] [북한·한미동맹] [방산·미래전 등] [국제·국제법 등]
  - 3️⃣ 법조·공직·수사기관 동향 — [국회·정부] [법조일반] [사법·수사기관]
  - 4️⃣ 🧑‍⚖️ 오늘의 주요 판결
  - 5️⃣ 🗓️ 오늘의 주요 일정 종합 (+ 고정 링크: 네이버 주요사설)
- 서브섹션별 최대 기사 수는 config에서 관리 (기본: 과거 사례 분량 수준)
- 시사 이슈용 임시 서브섹션(예: [형사소송법 개정 관련])은 config에 추가하는 방식으로 확장 가능

### 3.4 조판 (Formatter)
- 과거 작성 사례 포맷 재현: `🗞️ ['26. 8. 1.(금) 국방·법무 주요 뉴스 브리핑] 🗞️`, 섹션 이모지, `• 제목(언론사)` + 링크, 말미 `//끝//`
- 산출물 2종:
  - `newsletters/YYYY-MM-DD.md` — 저장소에 커밋되는 아카이브(카카오톡 링크 대상)
  - 발송용 플레인 텍스트 (텔레그램 본문)

### 3.5 발송 (Deliverer)
- **카카오톡 (1순위)**: `나에게 보내기` API. 텍스트 템플릿 200자 제한이 있으므로 *헤드라인 요약 + "전체 보기" 버튼(당일 뉴스레터 GitHub 링크)* 형태로 발송. 액세스 토큰은 리프레시 토큰으로 매회 자동 갱신, 갱신된 리프레시 토큰은 GitHub Secret에 자동 재저장(GH_PAT 설정 시) → 무인 운영 가능
- **텔레그램 (2순위)**: 봇 API로 **전문(full text)** 발송 (4,000자 단위 분할)
- 발송 모드 (env `DELIVERY_MODE`):
  - `all` (기본): 두 채널 모두 발송 — 카카오는 알림+링크, 텔레그램은 전문
  - `fallback`: 카카오 시도 → 실패 시에만 텔레그램
- 두 채널 모두 실패 시 워크플로 실패 처리(GitHub가 실패 메일 알림)

### 3.6 스케줄링 (06:30 수신 보장 설계)
GitHub Actions cron은 수 분~수십 분 지연될 수 있으므로:

1. **본 실행**: UTC 20:40 (KST 05:40) 시작 → 수집·분류·조판 완료 후 **KST 06:28까지 대기했다가 발송** (지연으로 06:28을 넘겨 시작된 경우 즉시 발송)
2. **백업 실행**: UTC 21:45 (KST 06:45) — 당일 뉴스레터가 이미 커밋되어 있으면 즉시 종료(중복 발송 방지), 없으면(본 실행 실패) 생성 후 즉시 발송
3. `workflow_dispatch`로 수동 실행 가능
4. 발송 시각·대기 상한은 env로 조정 가능 (`TARGET_SEND_TIME`, `MAX_WAIT_MIN`)

---

## 4. 시스템 아키텍처

```
GitHub Actions (cron 05:40 KST)
 ├─ 1. collect  : Google News RSS × 47 키워드 (병렬) ──> 원시 기사 목록
 ├─ 2. dedupe   : URL/제목 유사도 클러스터링(rapidfuzz) ──> 후보 ≤160건
 ├─ 3. classify : Claude API 1회 호출 (분류+의미중복+정렬, JSON 강제)
 ├─ 4. format   : 뉴스레터 조판 ──> newsletters/YYYY-MM-DD.md 커밋
 ├─ 5. wait     : KST 06:28까지 대기
 └─ 6. deliver  : 카카오 "나에게 보내기"(요약+링크) + 텔레그램(전문)
```

| 구성요소 | 파일 |
|---|---|
| 설정(키워드·섹션·상한·언론사순위) | `config.yaml` |
| 수집 | `src/collect.py` |
| 중복 제거 | `src/dedupe.py` |
| 분류 (Claude API) | `src/classify.py` |
| 조판 | `src/format_newsletter.py` |
| 발송 (카카오·텔레그램·시크릿 갱신) | `src/deliver.py` |
| 오케스트레이션 (generate / send) | `src/main.py` |
| 스케줄러 | `.github/workflows/newsletter.yml` |
| 카카오 최초 토큰 발급 도우미 | `scripts/kakao_auth.py` |

### 필요한 GitHub Secrets
| Secret | 용도 | 필수 |
|---|---|---|
| `ANTHROPIC_API_KEY` | Claude API | ✅ |
| `KAKAO_REST_API_KEY` | 카카오 앱 REST 키 | 카카오 사용 시 |
| `KAKAO_REFRESH_TOKEN` | 카카오 토큰 자동 갱신 | 카카오 사용 시 |
| `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` | 텔레그램 발송 | 텔레그램 사용 시 |
| `GH_PAT` | 카카오 리프레시 토큰 회전 시 Secret 자동 갱신 | 권장 |

---

## 5. 비기능 요구사항

- **비용**: 인프라 0원(공개 저장소 기준 Actions 무제한). Claude Haiku 기준 1회 ≈ 3~5만 입력 토큰 → 월 1,000~2,000원 수준
- **토큰/시간 상한**: LLM 입력은 제목+언론사만(본문 미전송), 후보 160건 상한, 호출 1회로 고정
- **안정성**: RSS 개별 키워드 실패는 건너뛰고 계속, 링크 복원 실패는 구글 링크로 폴백, 백업 크론으로 이중화
- **보안**: 모든 키는 GitHub Secrets, 저장소에 평문 저장 금지

---

## 6. 제약사항 및 리스크

| 리스크 | 내용 및 대응 |
|---|---|
| "가장 열람 많은 기사" 판단 | RSS는 조회수를 제공하지 않음 → **주요 언론사 순위 + 기사 상세도**를 대리 지표로 사용 (요구사항 3의 취지 반영) |
| naver.me 단축링크 | 네이버 비공식 단축 API가 없어 재현 불가 → 언론사 원문 링크 사용 |
| 카카오 텍스트 200자 제한 | 카카오는 요약+링크, 전문은 텔레그램/GitHub 아카이브로 제공 |
| GitHub Actions 지연 | 05:40 조기 시작 + 06:28 대기 발송 + 06:45 백업 크론 |
| 카카오 리프레시 토큰 만료(2개월) | 매 실행 시 갱신, 회전된 토큰 자동 재저장(GH_PAT). 미설정 시 텔레그램으로 갱신 안내 발송 |
| 구글 뉴스 링크 복원 로직 변경 | 서드파티 디코더 실패 시 구글 리다이렉트 링크로 폴백(클릭 시 정상 이동) |
| 사설 저장소일 경우 | Actions 무료 2,000분/월 한도 내 운영 가능하나 대기시간 포함 시 빠듯 → **공개 저장소 권장** 또는 대기시간 축소 |

---

## 7. 마일스톤

| 단계 | 내용 | 상태 |
|---|---|---|
| M1 | PRD 확정 | 본 문서 |
| M2 | 수집·중복제거·분류·조판 파이프라인 구현 | v1.0 포함 |
| M3 | 카카오·텔레그램 발송 + 토큰 자동 갱신 | v1.0 포함 |
| M4 | GitHub Actions 스케줄 + 백업 크론 | v1.0 포함 |
| M5 | 사용자 설정(카카오 앱 생성, Secrets 등록) — SETUP.md 참조 | 사용자 작업 |
| 추후 | 임시 서브섹션 운영, 수신자 확대(카카오 채널), 품질 튜닝 | 백로그 |
