# newsletter_v2 — 프로젝트 규칙

## 문서 동기화 (필수)

코드·설정(`config.yaml`)·워크플로(`.github/workflows/newsletter.yml`)를 변경했다면,
같은 작업 안에서 아래 세 문서 중 영향받는 부분을 함께 갱신할 것. 사용자가 매번 요청하지
않아도 자동으로 확인·반영한다.

| 문서 | 갱신 대상 |
|---|---|
| `PRD.md` | 요구사항·아키텍처·파이프라인 단계·config 키·GitHub Secrets·리스크가 바뀌었을 때 — "지금 구현을 그대로 재현할 수 있는 상세도" 유지가 기준 |
| `README.md` | 기능 요약·동작 방식 다이어그램·프로젝트 구조·설정표·비용·발송 채널표가 바뀌었을 때 |
| `SETUP.md` | 새 Secret·새 외부 서비스 연동·설정 절차·트러블슈팅이 추가/변경됐을 때 |

판단 기준: "이 변경으로 세 문서 중 하나라도 사실과 달라졌는가?" 그렇다면 함께 고친다.
변경이 순수 리팩터링이라 사용자 행동/설정/아키텍처에 영향이 없다면 문서를 건드릴 필요는 없다.

## 검증 관례

- 이 저장소는 GitHub Actions(`daily-newsletter` 워크플로)로 매일 자동 실행된다. 코드 변경 후
  실제 동작을 확인하려면: 오늘자 `newsletters/YYYY-MM-DD.md`가 이미 있으면 지우고 커밋 →
  `workflow_dispatch`로 재실행 → 로그 확인(당일분이 존재하면 생성 단계가 스킵되어 가짜 성공이 될 수 있음).
- Kakao·Notion·Telegram·Gemini는 모두 필수 Secret이 없으면 그레이스풀 스킵하도록 설계되어
  있다 — 이 패턴을 새 선택적 연동에도 유지할 것.
